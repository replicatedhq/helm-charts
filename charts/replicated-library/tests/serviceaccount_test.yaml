suite: test serviceaccount
values:
  - ./values/serviceaccount.yaml
templates:
  - test_serviceaccount.yaml
tests:
  - it: should work
    asserts:
      - isKind:
          of: ServiceAccount
      - matchRegex:
          path: metadata.name
          pattern: example
