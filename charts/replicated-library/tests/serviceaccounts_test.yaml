suite: test serviceaccounts
values:
  - ./values/serviceaccounts.yaml
templates:
  - test_serviceaccounts.yaml
tests:
  - it: should work
    asserts:
      - isKind:
          of: ServiceAccount
      - equal:
          path: metadata.name
          value: RELEASE-NAME-replicated-library-example1
      - matchRegex:
          path: metadata.name
          pattern: example1
        documentIndex: 0
